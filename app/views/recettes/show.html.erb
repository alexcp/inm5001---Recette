<%= link_to 'Retour', recettes_path, :class=>"btn"%>
<%= link_to 'Modifier', edit_recette_path(@recette), :class=>"btn btn-info"%>
<%= link_to 'Ajouter aux favoris', @recette, :class=>"btn btn-inverse"%>

<div class="row">
  <span class="span2">
    <a href="/vote_up/<%= @recette.id %>" class="btn <%= "btn-info" if current_user.voted_up_on? @recette %>">
    <i class="icon-thumbs-up"> </i><%= @recette.likes.count %>
  </a>
  <a href="/vote_down/<%= @recette.id %>" class="btn <%= "btn-info" if current_user.voted_down_on? @recette %>">
    <i class="icon-thumbs-down"> </i><%= @recette.dislikes.count %>
  </a>
  </span>
  <span class="span10">
    <h1><%= @recette.titre.titleize %></h1>
    <%= @recette.description %>
  </span>
</div>

<div class="row">
  <div class="span8">

    <br>
    <a href="#img_modal" data-toggle="modal"><%= image_tag @recette.photo.url(:medium), :class => "img_preview" %></a>
    <h3>Description</h3>
    <hr />
    <%= @recette.description %>

    <h3>Prépararion</h3>
    <%= @recette.preparation %>
  </div>
  <div class="span4">
    <h3>Ingrédients</h3>
    <hr />
      <table class="table table-striped">
    <% @recette.ingredient_recettes.each do |ingredient| %>
      <tr>
        <td>
          <%= ingredient.nom %>
        </td>
        <td>
          <%= ingredient.quantite %> x <%= ingredient.unite_de_mesure %>
      </tr>
    <% end %>
      </table>
  </div>
</div>
<h3>Commentaires</h3>
<form action="/comment/<%= @recette.id %>" >
  <div><%=text_area_tag 'body', nil, :size => "10x2", :class =>"resizable" %></div>
  <div><%=submit_tag "Commenter", :class=>"btn btn-info" %></div>
</form>
<table class="table table-striped">
  <% @recette.root_comments.find(:all, order: "created_at DESC").each do |commentaire| %>
     <tr>
        <td>
         <span class="user_comment"> <%= User.find(commentaire.user_id).name %> </span>
        </td>
        <td>
          <%= commentaire.body %>
      </tr>
  <% end %>
</table>
<br/>
<br/>
<%= link_to 'Modifier', edit_recette_path(@recette), :class=>"btn btn-info"%>
<%= link_to 'Retour', recettes_path, :class=>"btn"%>
<br />
<hr />



<div id="img_modal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-body">
  <button type="button" class="close" data-dismiss="modal" aria-hidden="true">x</button>
  <%= image_tag @recette.photo.url(:large)%>
</div>
</div>

